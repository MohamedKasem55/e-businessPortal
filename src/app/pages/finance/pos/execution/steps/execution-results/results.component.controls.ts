import { FormModel } from "app/@core/model/dto/formModel"

export const getExecutionResultsControl = () => {
    return new FormModel({
        id: 'executionResults',
        controls: {
           
        }
    })
}
